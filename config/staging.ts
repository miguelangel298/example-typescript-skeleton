const config = {
  port: 3000,
  /**
   * Database
   */
  database: {
    synchronize: false,
    host: 'localhost',
    username: 'username',
    password: 'password',
    database: 'database',
  },
};
export default config;
